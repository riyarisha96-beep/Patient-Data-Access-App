{
  "name": "patient-data-access-blockchain",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "hardhat": "^2.22.5",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5"
  },
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy:local": "hardhat run --network localhost scripts/deploy.js",
    "test": "hardhat test"
  }
}